kind = "web"
previewPath = "/"
title = "Fortify"
version = "1.0.0"
id = "artifacts/fortify"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
localPort = 21405
name = "web"
paths = ["/"]

[services.development]
run = "pnpm --filter @workspace/fortify run dev"

[services.env]
PORT = "21405"
BASE_PATH = "/"

[services.production]
build = ["pnpm", "--filter", "@workspace/fortify", "run", "build"]
publicDir = "artifacts/fortify/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"
